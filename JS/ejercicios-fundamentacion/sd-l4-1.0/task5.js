export class Player {
  constructor(name, level, exPoints) {
    this.name = name;
    this.level = level;
    this.exPoints = exPoints; 
  }

  info() {
    console.log(this.name + " has reached Level " + this.level + "!");
  }

  levelUp(points) {

    }
    this.level += 1;

  }

  levelExperience(points){
    this.exPoints += poinst;
    switch(this.exPoints){
      case (this.exPoints < 10)
    }

  }


  
  
}